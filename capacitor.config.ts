import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'examen_ChavarriaLinoKassandra',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
